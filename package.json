{
  "name": "helios-selene",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
      "build:viverse": "vite build --base=./",
    "build:github-pages": "vite build --base=/helios-selene/",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.185.1"
  },
  "devDependencies": {
    "vite": "^8.1.1"
  }
}